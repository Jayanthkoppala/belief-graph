{
  "name": "packet-graph",
  "version": "1.0.0",
  "description": "Consent-scoped context packets for hiring agents, on a HydraDB context graph",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "spike": "tsx --env-file=.env src/spike.ts"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@hydradb/sdk": "^2.1.2"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
